/**
 * Node half of dsh-mobile.
 *
 * The bundle needs a Loader row so the official dsh client-module scanner can
 * discover package.json#dsh.client. Remote transport, sessions and permissions
 * remain owned by the shipped Web profile.
 */
export function apply(): void {}
